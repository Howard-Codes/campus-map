name: Node.js CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
        - name: Use Node.js 14.x
          uses: actions/setup-node@v2
          with:
            node-version: 14.x
        - name: Build
          run: npm run build
